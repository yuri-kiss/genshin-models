'use strict';

export const main = document.querySelector('main')!;
export const loadingDiv = document.querySelector('main > div#loading')!;
export const appDiv = document.querySelector('main > div#app')!;
